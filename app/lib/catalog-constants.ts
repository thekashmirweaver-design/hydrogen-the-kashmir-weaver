/** Products per grid page / infinite-scroll fetch. */
export const PRODUCT_LIST_PAGE_SIZE = 24;

/** Batch size when assembling the full catalog snapshot (background fetches). */
export const PRODUCT_FETCH_BATCH_SIZE = 50;
