# ── Fastest setup ─────────────────────────────────────────────────────────────
# npx shopify hydrogen link
# npx shopify hydrogen env pull
# Most Hydrogen/Oxygen vars below are filled automatically after link + pull.
# Generate SESSION_SECRET yourself: openssl rand -hex 32

# ── Required (Oxygen / local dev) ─────────────────────────────────────────────
SESSION_SECRET=
# Storefront API public token (from Headless channel / hydrogen env pull).
PUBLIC_STOREFRONT_API_TOKEN=
# Enable `unauthenticated_read_product_inventory` on the Headless storefront app
# (Shopify Admin → Sales channels → Hydrogen → Storefront settings → Storefront API)
# or see shopify.storefront.toml + `npm run storefront:push-scopes`.
# Without it, untracked products still work; tracked qty caps fall back to custom.stock_qty metafields.

# Optional server-side private token. Leave unset to use PUBLIC token (recommended
# unless you have a distinct private token from hydrogen env pull).
# NEVER put an Admin shpat_ token here — Hydrogen sends Shopify-Storefront-Id and
# invalid private tokens cause 401 → static catalog fallback.
# PRIVATE_STOREFRONT_API_TOKEN=
PUBLIC_STORE_DOMAIN=
PUBLIC_STOREFRONT_ID=
PUBLIC_CHECKOUT_DOMAIN=

# ── Customer Account API (account login) ──────────────────────────────────────
# Pulled automatically by `npx shopify hydrogen env pull` after linking.
# Required on Oxygen for /account login; missing vars cause auth failures.
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=
PUBLIC_CUSTOMER_ACCOUNT_API_URL=
SHOP_ID=

# ── Optional storefront behaviour ─────────────────────────────────────────────
# Keep true until npm run seed:shopify completes, then remove or set false.
# Also gates static journal demo posts (homepage + /journal) when Shopify has no articles.
USE_STATIC_CATALOG=true
# Concierge form → Gmail (via Resend). Create key at https://resend.com/api-keys
# and verify thekashmirweaver.in (or your sending domain) in Resend.
RESEND_API_KEY=
# Defaults to thekashmirweaver@gmail.com when unset.
CONCIERGE_EMAIL_TO=
# Must use a domain verified in Resend.
CONCIERGE_EMAIL_FROM=The Kashmir Weaver Concierge <concierge@thekashmirweaver.in>

# ── Seed script only (scripts/seed-shopify-admin.ts) ──────────────────────────
# Admin → Settings → Apps → Develop apps → Create app → Admin API token
SHOPIFY_ADMIN_ACCESS_TOKEN=
# Canonical storefront URL (CSP + seed scripts). Use the primary custom domain when live.
PUBLIC_STORE_URL=https://thekashmirweaver.in
